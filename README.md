# Account Report Transform (Streamlit)

เว็บสำหรับแปลงรายงานใบกำกับสินค้า  
- Auto-detect header (skiprows)
- รองรับหลายไฟล์
- รวมผลลัพธ์เป็น Excel / CSV

## Run locally
```bash
pip install -r requirements.txt
streamlit run bill.py
